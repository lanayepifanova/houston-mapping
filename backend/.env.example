# Backend environment
PORT=4000
DATABASE_URL=file:../data/dev.db
# Production example (Render persistent disk):
# DATABASE_URL=file:/var/data/prod.db
# Allow multiple origins for dev and previews (comma-separated)
CORS_ORIGIN=http://localhost:5173,http://localhost:4173
OPENAI_API_KEY=
